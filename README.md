# tut-spring-boot-docker
An example of Dockerized Spring Boot application

Build app

    ./gradlew build
  
Run the app

    java -jar build/libs/tut-spring-boot-docker-0.1.0.jar

Available Endpoints:

    /hello
    /users/{userId}
